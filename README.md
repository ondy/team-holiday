# team-holiday

## Entwicklung

```sh
npm install
npm run dev
```

## Build für GitHub Pages

```sh
npm run build
```

Der Build liegt im Ordner `dist/`. Die Vite-Basis ist auf relative Pfade (`./`) gesetzt,
damit die Seite direkt aus einem GitHub-Pages-Unterverzeichnis funktioniert.
